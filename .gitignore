/bazel*
/.vscode
.cache
.DS_Store
compile_commands.json
MODULE.bazel.lock
/build
/cmake-build*
/out
.bazelrc

# Jekyll (GitHub Pages) — only relevant for local previews; CI builds
# happen in an ephemeral runner and never commit these back.
/_site
/.jekyll-cache
/.jekyll-metadata
/vendor
/Gemfile.lock